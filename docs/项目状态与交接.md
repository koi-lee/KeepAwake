# KeepAwake 项目状态与交接

## 一句话现状

KeepAwake 当前在 `main` 分支，最新提交 `63dfe68`；本地有未提交的合盖授权失败诊断改进，尚未发布。

## 接手三步

1. `swift test`（当前沙箱因 SwiftPM 用户缓存访问限制未能运行；需在可用开发环境重试）。
2. `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project KeepAwake.xcodeproj -scheme KeepAwake -configuration Debug -derivedDataPath /private/tmp/KeepAwake-DerivedData CODE_SIGNING_ALLOWED=NO build`。
3. 发布前按 `DEPLOYMENT.md` 操作；本次没有提交、推送或发布授权。

## 已完成

- 2026-09-25：针对 M1 / macOS Tahoe 26.6.2 用户录屏中的授权失败弹窗，改进授权失败诊断和 Mac 登录密码指引；新增错误消息回归测试；Xcode Debug 构建通过。详见 `outputs/authorization-20260925/RESULT.md`。

## 进行中

- 尚未在用户妻子的电脑上复测授权；也未验证其授权弹窗是否出现、使用的安装包签名/来源或失败时的具体系统返回码。

## 下一步

1. 用户侧使用本次本地构建或后续签名包复测授权，并记录系统密码框是否出现及失败提示。
2. 在有 SwiftPM 用户缓存访问能力的环境中执行 `swift test`。
3. 根据实机结果决定是否需要进一步调整授权实现。

## 已知坑与环境注意

- 现有合盖授权通过 AppleScript `do shell script ... with administrator privileges` 运行长期 shell 循环；失败的原因可能是授权取消、系统未显示密码框或 pmset/握手失败，不能仅凭录屏确定是哪一种。
- 录屏显示的是通用失败提示，截图系统为 Apple M1、macOS Tahoe 26.6.2。当前工程构建目标 macOS 13+，但真实授权流程仍需目标机验证。
- `swift test` 在当前受限沙箱内被 `sandbox_apply: Operation not permitted` 阻止，Xcode build 可成功。

## 文档索引

- `README.md`：用户功能及限制
- `TESTING.md`：验证路径
- `DEPLOYMENT.md`：发布流程
- `docs/项目流程图.md`：应用流程
- `outputs/authorization-20260925/RESULT.md`：本次证据
